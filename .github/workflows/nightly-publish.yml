name: Publish Pre-release Extension

on:
  push:
    branches: [master]
  workflow_dispatch: # Allows manual triggering.

permissions:
  contents: read

concurrency:
  group: marketplace-prerelease
  cancel-in-progress: false

jobs:
  publish-prerelease:
    runs-on: ubuntu-latest
    environment: marketplace-prerelease
    # Expose the marketplace tokens as job-level env so steps can gate on them
    # with `if: env.* != ''` — GitHub Actions does NOT allow the `secrets`
    # context inside `if:` expressions (workflow parse error otherwise).
    env:
      VSCE_PAT: ${{ secrets.VSCE_PAT }}
      OVSX_PAT: ${{ secrets.OVSX_PAT }}

    steps:
      - name: Checkout code
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          # The VSIX embeds modes regenerated from the custom-modes submodule at
          # build time, so CI MUST check it out at the recorded pin — otherwise the
          # build would silently reuse stale committed artifacts.
          submodules: recursive

      - name: Setup Node.js and pnpm
        uses: ./.github/actions/setup-node-pnpm
        with:
          install-args: "--frozen-lockfile"

      - name: Verify custom-modes submodule pin & committed artifact sync
        run: |
          echo "::group::[CI:NIGHTLY:SUBMODULE] Verifying submodule pin + .roomodes sync"
          node scripts/verify-submodule-pin.mjs
          node scripts/verify-roomodes-sync.mjs
          echo "::notice::[CI:NIGHTLY:SUBMODULE] submodule pin and .roomodes sync verified"
          echo "::endgroup::"

      - name: Validate package identity
        run: |
          echo "::group::[CI:NIGHTLY:IDENTITY] Validating package identity"
          package_name=$(node -p "require('./src/package.json').name")
          publisher=$(node -p "require('./src/package.json').publisher")

          test "$package_name" = "roo-plus"
          test "$publisher" = "xavier-arosemena"
          echo "::notice::[CI:NIGHTLY:IDENTITY] package identity OK: $package_name / $publisher"
          echo "::endgroup::"

      - name: Validate publish ref
        run: |
          echo "::group::[CI:NIGHTLY:REF] Validating publish ref"
          if [ "$GITHUB_REF_NAME" != "master" ]; then
            echo "::error::[CI:NIGHTLY:REF] Pre-release publishes must run from master, not ${GITHUB_REF_NAME}."
            exit 1
          fi
          echo "::notice::[CI:NIGHTLY:REF] publish ref OK: $GITHUB_REF_NAME"
          echo "::endgroup::"

      - name: Validate VS Code Marketplace PAT (if configured)
        env:
          VSCE_PAT: ${{ secrets.VSCE_PAT }}
        run: |
          echo "::group::[CI:NIGHTLY:VSCE-PAT] Validating VS Code Marketplace PAT"
          if [ -z "$VSCE_PAT" ]; then
            echo "::warning::[CI:NIGHTLY:VSCE-PAT] VSCE_PAT is not configured — the VS Code Marketplace pre-release publish step will be skipped."
            echo "::notice::[CI:NIGHTLY:VSCE-PAT] To enable it, add a Personal Access Token (Manage scope, publisher 'xavier-arosemena') as the VSCE_PAT secret in the marketplace-prerelease environment."
            echo "::endgroup::"
            exit 0
          fi
          if ! npx @vscode/vsce verify-pat xavier-arosemena > /tmp/vsce-pat.log 2>&1; then
            echo "::error::[CI:NIGHTLY:VSCE-PAT] VSCE_PAT is invalid, expired, or lacks 'Manage' permission for publisher 'xavier-arosemena'."
            echo "::error::[CI:NIGHTLY:VSCE-PAT] Regenerate a PAT at https://marketplace.visualstudio.com/manage/publishers/xavier-arosemena (Marketplace -> Manage)."
            echo "::error::[CI:NIGHTLY:VSCE-PAT] Update the VSCE_PAT secret in the marketplace-prerelease environment, then re-run."
            cat /tmp/vsce-pat.log
            exit 1
          fi
          echo "::notice::[CI:NIGHTLY:VSCE-PAT] VS Code Marketplace PAT verified for publisher xavier-arosemena"
          echo "::endgroup::"

      - name: Validate Open VSX access token (if configured)
        env:
          OVSX_PAT: ${{ secrets.OVSX_PAT }}
        run: |
          echo "::group::[CI:NIGHTLY:OVSX-PAT] Validating Open VSX access token"
          if [ -z "$OVSX_PAT" ]; then
            echo "::warning::[CI:NIGHTLY:OVSX-PAT] OVSX_PAT is not configured — the Open VSX pre-release publish step will be skipped."
            echo "::notice::[CI:NIGHTLY:OVSX-PAT] To enable it, create an access token at https://open-vsx.org/user-settings/tokens (publisher 'xavier-arosemena') and add it as the OVSX_PAT secret in the marketplace-prerelease environment."
            echo "::endgroup::"
            exit 0
          fi
          echo "::notice::[CI:NIGHTLY:OVSX-PAT] Open VSX access token configured"
          echo "::endgroup::"

      - name: Skip if this push is a release merge
        id: release-check
        run: |
          echo "::group::[CI:NIGHTLY:RELEASE-CHECK] Checking whether this push is a release merge"
          commit_subject=$(git log -1 --format=%s)
          skip=false

          if [[ "$commit_subject" =~ ^chore:\ prepare\ v[0-9]+\.[0-9]+\.[0-9]+\ release ]]; then
            echo "::notice::[CI:NIGHTLY:RELEASE-CHECK] Commit '${commit_subject}' looks like a release-prep merge; skipping nightly pre-release."
            skip=true
          fi

          echo "skip=${skip}" >> "$GITHUB_OUTPUT"
          echo "::notice::[CI:NIGHTLY:RELEASE-CHECK] skip=${skip}"
          echo "::endgroup::"

      - name: Set pre-release version
        if: steps.release-check.outputs.skip != 'true'
        id: version
        env:
          RUN_NUMBER: ${{ github.run_number }}
        run: |
          echo "::group::[CI:NIGHTLY:VERSION] Setting pre-release version"
          node <<'EOF'
          const fs = require("fs")
          const pkgPath = "src/package.json"
          const pkg = JSON.parse(fs.readFileSync(pkgPath, "utf8"))
          const [major, minor] = pkg.version.split(".").map(Number)
          const prereleaseMinor = minor % 2 === 0 ? minor + 1 : minor + 2
          const patch = 100000 + Number(process.env.RUN_NUMBER)

          pkg.version = `${major}.${prereleaseMinor}.${patch}`
          fs.writeFileSync(pkgPath, `${JSON.stringify(pkg, null, "\t")}\n`)
          fs.appendFileSync(process.env.GITHUB_OUTPUT, `number=${pkg.version}\n`)
          console.log(`Pre-release version set to ${pkg.version}`)
          EOF
          echo "::notice::[CI:NIGHTLY:VERSION] pre-release version set"
          echo "::endgroup::"

      - name: Build workspace packages
        if: steps.release-check.outputs.skip != 'true'
        env:
          PKG_RELEASE_CHANNEL: prerelease
        run: |
          echo "::group::[CI:NIGHTLY:BUILD] Building workspace packages"
          pnpm --filter @roo-code/build build
          pnpm --filter @roo-code/vscode-webview build
          echo "::notice::[CI:NIGHTLY:BUILD] workspace packages built"
          echo "::endgroup::"

      - name: Package pre-release VSIX
        if: steps.release-check.outputs.skip != 'true'
        env:
          POSTHOG_API_KEY: ${{ secrets.POSTHOG_API_KEY }}
          PKG_RELEASE_CHANNEL: prerelease
        run: |
          echo "::group::[CI:NIGHTLY:PACKAGE] Packaging pre-release VSIX"
          mkdir -p bin
          pnpm --filter ./src exec vsce package --pre-release --no-dependencies --out ../bin
          echo "::notice::[CI:NIGHTLY:PACKAGE] pre-release VSIX packaged"
          echo "::endgroup::"

      - name: Verify VSIX contents
        if: steps.release-check.outputs.skip != 'true'
        env:
          VERSION_NUMBER: ${{ steps.version.outputs.number }}
        run: |
          echo "::group::[CI:NIGHTLY:VERIFY-VSIX] Verifying VSIX contents"
          vsix_path="bin/roo-plus-${VERSION_NUMBER}.vsix"
          unzip -l "$vsix_path" > /tmp/roo-plus-vsix-contents.txt
          grep -q "extension/package.json" /tmp/roo-plus-vsix-contents.txt
          grep -q "extension/dist/extension.js" /tmp/roo-plus-vsix-contents.txt
          grep -q "extension/webview-ui/build/assets/index.js" /tmp/roo-plus-vsix-contents.txt
          grep -q "extension/webview-ui/audio/celebration.wav" /tmp/roo-plus-vsix-contents.txt
          echo "::notice::[CI:NIGHTLY:VERIFY-VSIX] VSIX contents verified"
          echo "::endgroup::"

      - name: Validate packaged manifest identity
        if: steps.release-check.outputs.skip != 'true'
        env:
          VERSION_NUMBER: ${{ steps.version.outputs.number }}
        run: |
          echo "::group::[CI:NIGHTLY:MANIFEST] Validating packaged manifest identity"
          vsix_path="bin/roo-plus-${VERSION_NUMBER}.vsix"

          artifact_name=$(unzip -p "$vsix_path" extension/package.json | jq -r '.name')
          artifact_publisher=$(unzip -p "$vsix_path" extension/package.json | jq -r '.publisher')

          test "$artifact_name" = "roo-plus"
          test "$artifact_publisher" = "xavier-arosemena"
          echo "::notice::[CI:NIGHTLY:MANIFEST] packaged manifest identity OK: $artifact_name / $artifact_publisher"
          echo "::endgroup::"

      - name: Publish pre-release to VS Code Marketplace
        if: steps.release-check.outputs.skip != 'true' && env.VSCE_PAT != ''
        env:
          VERSION_NUMBER: ${{ steps.version.outputs.number }}
        run: |
          echo "::group::[CI:NIGHTLY:PUBLISH:VSCE] Publishing pre-release to VS Code Marketplace"
          npx @vscode/vsce publish --pre-release --skip-duplicate --packagePath "bin/roo-plus-${VERSION_NUMBER}.vsix"
          echo "::notice::[CI:NIGHTLY:PUBLISH:VSCE] Published or skipped existing xavier-arosemena.roo-plus ${VERSION_NUMBER} as a VS Code Marketplace pre-release"
          echo "::endgroup::"

      # The VSIX built above with `vsce package --pre-release` already carries the
      # Microsoft.VisualStudio.Code.PreRelease manifest property, which is what Open
      # VSX reads to flag the version. `ovsx publish` ignores --pre-release for an
      # already-packaged .vsix (it only applies when ovsx does the packaging itself),
      # so it's intentionally omitted here.
      #
      # Open VSX's "latest" alias resolves to the highest semver across stable and
      # pre-release alike (pre-release only breaks ties at equal major.minor.patch),
      # so a nightly build can transiently become "latest" until the next stable
      # release outranks it. This mirrors how Marketplace pre-release users already
      # track the newest published version, so it's an accepted trade-off here too.
      - name: Publish pre-release to Open VSX Registry
        if: steps.release-check.outputs.skip != 'true' && env.OVSX_PAT != ''
        env:
          VERSION_NUMBER: ${{ steps.version.outputs.number }}
        run: |
          echo "::group::[CI:NIGHTLY:PUBLISH:OVSX] Publishing pre-release to Open VSX Registry"
          set -o pipefail
          publish_output=$(pnpm exec ovsx publish "bin/roo-plus-${VERSION_NUMBER}.vsix" --skip-duplicate 2>&1 | tee /dev/stderr)
          if echo "$publish_output" | grep -q "is already published"; then
            echo "::notice::[CI:NIGHTLY:PUBLISH:OVSX] xavier-arosemena.roo-plus ${VERSION_NUMBER} was already published to Open VSX; skipped."
          else
            echo "::notice::[CI:NIGHTLY:PUBLISH:OVSX] Published xavier-arosemena.roo-plus ${VERSION_NUMBER} as an Open VSX pre-release"
          fi
          echo "::endgroup::"
